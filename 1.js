// 1. Merge Intervals
// Given an array of intervals where intervals[i] = [start_i, end_i],
// merge all overlapping intervals and return an array of the non-overlapping intervals.
const intervals1 = [[1,3],[2,6],[8,10],[15,18]];
// Expected output: [[1,6],[8,10],[15,18]]
function mergeIntervals(intervals) {
  
}

// 2. Container With Most Water
// Given an array of non-negative integers height where each element represents a point at coordinate (i, height[i]),
// n vertical lines are drawn such that the two endpoints of the ith line are at (i, 0) and (i, height[i]).
// Find two lines that together with the x-axis form a container, such that the container contains the most water.
const heights = [1,8,6,2,5,4,8,3,7];
// Expected output: 49
function maxArea(height) {
  // Your code here
}

// 3. Subarray Sum Equals K
// Given an array of integers nums and an integer k,
// return the total number of continuous subarrays whose sum equals to k.
const nums1 = [1,1,1];
const k1 = 2;
// Expected output: 2
function subarraySum(nums, k) {
  // Your code here
}

// 4. Product of Array Except Self
// Given an integer array nums, return an array answer such that answer[i]
// is equal to the product of all the elements of nums except nums[i].
// The product of any prefix or suffix of nums is guaranteed to fit in a 32-bit integer.
// You must write an algorithm that runs in O(n) time and without using the division operation.
const nums2 = [1,2,3,4];
// Expected output: [24,12,8,6]
function productExceptSelf(nums) {
  // Your code here
}

// 5. Top K Frequent Elements
// Given an integer array nums and an integer k,
// return the k most frequent elements. You may return the answer in any order.
const nums3 = [1,1,1,2,2,3];
const k2 = 2;
// Expected output: [1,2]
function topKFrequent(nums, k) {
  // Your code here
}
